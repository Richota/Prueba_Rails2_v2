<h3><%= @task.name %></h3>

<% i = 0 %>
<div class="jumbotron text-center">
  <%= image_tag @task.photo, class: "img-fluid img-thumbnail" %>
</div>
  <br>
  <div class="row">
    <div class="col-md-6">
      <div class="card bg-primary">
        <table class="table table-hover">
          <thead>
            <tr>
              <th>  </th>
              <th>Lista de usuarios Ok</th>
            </tr>
          </thead>
          <tbody>
            <% @task.users.each do |u| %>
            <tr>
              <td> <%= i + 1%></td>
              <td>
                <%= u.name %>
              </td>
            </tr>
            <% end  %>
          </tbody>
        </table>
      </div>
    </div>

    <div class="col-md-6">
      <div class="card bg-primary">
        <table class="table table-hover">
          <thead>
            <tr>
              <th>TOP 5</th>

            </tr>
          </thead>
          <tbody>
            <% @task.users.each do |u| %>
            <tr>
              <td> <%= image_tag u.photo, class: "img-fluid img-thumbnail"%></td>
              <td>
                <%= u.name %>
              </td>
            </tr>
            <% end  %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
